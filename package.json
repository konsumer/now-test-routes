{
  "name": "now-test-routes",
  "version": "0.0.0",
  "scripts": {
    "deploy": "now",
    "start": "now dev",
    "postinstall": "cd api && npm i && cd ../front && npm i"
  },
  "devDependencies": {
    "now": "^16.4.0"
  }
}
